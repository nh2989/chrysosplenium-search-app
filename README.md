# 日本産ネコノメソウ属検索表アプリ

> いがりまさし『日本のネコノメソウ』（2024）掲載の検索表をベースにした、スマートフォン対応の種同定Webアプリ。

🌐 **公開URL**: `https://nh2989.github.io/chrysosplenium-search-app/`

---

## 概要

| 項目 | 内容 |
|------|------|
| 対象分類群 | ネコノメソウ属 *Chrysosplenium* L.（ユキノシタ科） |
| 収録種数 | 20種（日本産全種） |
| 参考文献 | いがりまさし『日本のネコノメソウ』236p |
| 設計ベース | 日本産しだ主要属検索表アプリ（同一構成） |
| 動作環境 | モダンブラウザ全般・PWA対応（オフライン利用可） |

---

## ファイル構成

```
nekonome/
├── .githooks/
│   └── pre-commit        # コミット時に CACHE_VERSION を自動更新
├── .nojekyll             # Jekyll 処理を無効化（GitHub Pages 必須）
├── index.html            # エントリーポイント
├── app.js                # メインロジック
├── style.css             # スタイルシート
├── manifest.json         # PWA設定
├── sw.js                 # Service Worker（オフライン対応）
├── icon-192.png          # PWAアイコン（192×192px）
├── icon-512.png          # PWAアイコン（512×512px）
├── README.md             # このファイル
└── data/
    ├── genera.js         # 属一覧データ（GENERA_DATA）
    └── chrysosplenium.js # 検索ツリー＋種情報データ
```

### データの分離設計

`app.js` はデータを持たない純粋なロジック層。属データは `data/` フォルダに分離し、検索開始時に動的ロードされる。将来的に属を追加する際は `data/` にJSファイルを追加し、`genera.js` に1行追記するだけでよい。

```js
GENUS_TREES['chrysosplenium'] = { ... }   // 検索ツリー
SPECIES_DATA['chrysosplenium'] = { ... }  // 種情報（学名・分布など）
```

---

## GitHub Pages への公開手順

### 1. リポジトリを作成してプッシュ

```bash
git init
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/nh2989/chrysosplenium-search-app.git
git push -u origin main
```

### 2. GitHub Pages を有効化

1. リポジトリページ → **Settings** タブを開く
2. 左メニュー → **Pages** をクリック
3. **Source** を以下のように設定して **Save**

```
Build and deployment
  Source:  Deploy from a branch
  Branch:  main  /  (root)
```

4. 数分後に `https://nh2989.github.io/chrysosplenium-search-app/` で公開される

> ⚠️ `.nojekyll` ファイルが必須。ないと `data/` フォルダ内の JS ファイルが 404 になる。

---

### 公開後の動作確認チェックリスト

- [ ] トップページが表示される
- [ ] 検索ツリーが正常に動作する（属カード → 検索開始）
- [ ] 同定結果に分布情報が表示される
- [ ] ブラウザの開発ツール → **Application** → **Service Workers** で SW が登録されている
- [ ] スマートフォンで「ホーム画面に追加」から PWA インストールできる
- [ ] 機内モード（オフライン）でも動作する

---

### CACHE_VERSION の自動更新（初回のみ設定）

コミットのたびに `sw.js` の `CACHE_VERSION` を日時で自動更新する Git フックを用意している。
リポジトリをクローン・初期化した後、以下のコマンドを **1回だけ** 実行する。

```bash
# フックを有効化（リポジトリのルートで実行）
git config core.hooksPath .githooks
chmod +x .githooks/pre-commit
```

設定後は `git commit` のたびに自動で以下のように更新される。

```
✅ CACHE_VERSION を v20260506-1430 に更新しました
```

### コンテンツ更新時の手順

フック設定済みであれば `CACHE_VERSION` の手動変更は不要。

```bash
# 1. ファイルを編集する

# 2. コミット（CACHE_VERSION が自動更新される）
git add .
git commit -m "データ更新: 変種を追加"

# 3. プッシュ
git push
```

---

## ローカルでの開発

Service Worker を有効にするには `file://` ではなく HTTP サーバー経由で開く必要がある。

```bash
# VSCode の Live Server 拡張機能（推奨）
# index.html を右クリック → "Open with Live Server"

# Python の簡易サーバー
cd nekonome
python3 -m http.server 8000
# → http://localhost:8000 をブラウザで開く
```

---

## 収録種一覧

### Sect.1 ネコノメソウ節（茎葉対生）

#### Ser.1 マルバネコノメソウ列

| 種名 | 学名 | 参照 | 北海道 | 本州 | 四国 | 九州 |
|------|------|------|:------:|:----:|:----:|:----:|
| マルバネコノメソウ | *C. ramosum* | P32 | ● | ◐ | — | — |

#### Ser.2 ネコノメソウ列（種子に隆条なし）

| 種名 | 学名 | 参照 | 北海道 | 本州 | 四国 | 九州 |
|------|------|------|:------:|:----:|:----:|:----:|
| ネコノメソウ | *C. grayanum* | P38 | ● | ● | — | ◐ |
| ヒメオオイワボタン | *C. pseudofauriei* var. *nipponicum* | P44 | — | — | ◐ | — |

#### Ser.3 チシマネコノメソウ列（無毛・隆条に突起なし）

| 種名 | 学名 | 参照 | 北海道 | 本州 | 四国 | 九州 |
|------|------|------|:------:|:----:|:----:|:----:|
| チシマネコノメソウ | *C. kamtschaticum* | P48 | ● | ◐ | — | — |

#### Ser.4 イワボタン列（無毛・隆条に突起あり）

| 種名 | 学名 | 参照 | 北海道 | 本州 | 四国 | 九州 |
|------|------|------|:------:|:----:|:----:|:----:|
| イワネコノメソウ | *C. echinus* | P56 | — | ◐ | ● | ● |
| ホクリクネコノメ | *C. fauriei* | P60 | — | ◐ | — | — |
| ボタンネコノメソウ | *C. kiotoense* | P68 | — | ◐ | — | — |
| ヒダボタン | *C. nagasei* | P76 | — | ◐ | — | — |
| スズカボタン | *C. suzukaense* | P88 | — | ◐ | — | — |
| イワボタン | *C. macrostemon* | P92 | — | ◐ | ● | ● |
| ヘイケボタン | *C. brevistamineum* | P122 | — | ◐ | — | — |

#### Ser.5 シロバナネコノメソウ列（毛あり）

| 種名 | 学名 | 参照 | 北海道 | 本州 | 四国 | 九州 |
|------|------|------|:------:|:----:|:----:|:----:|
| ムカゴネコノメソウ | *C. maximowiczii* | P130 | — | ◐ | — | — |
| シロバナネコノメソウ | *C. album* | P134 | — | ◐ | ● | ● |
| ツクシネコノメソウ | *C. rhabdospermum* | P156 | — | — | ◐ | ● |
| トウノウネコノメ | *C. pseudopilosum* | P162 | — | ◐ | — | — |
| コガネネコノメソウ | *C. sphaerospermum* | P170 | — | ◐ | ● | ● |

### Sect.2 ヤマネコノメソウ節（葉互生）

#### Ser.6 ヤマネコノメソウ列

| 種名 | 学名 | 参照 | 北海道 | 本州 | 四国 | 九州 |
|------|------|------|:------:|:----:|:----:|:----:|
| ヤマネコノメソウ | *C. japonicum* | P180 | ◐ | ● | ● | ● |
| エゾネコノメソウ | *C. alternifolium* var. *sibiricum* | P184 | ◐ | — | — | — |
| タチネコノメソウ | *C. tosaense* | P188 | — | ◐ | ● | ● |

#### Ser.7 ツルネコノメソウ列

| 種名 | 学名 | 参照 | 北海道 | 本州 | 四国 | 九州 |
|------|------|------|:------:|:----:|:----:|:----:|
| ツルネコノメソウ | *C. flagelliferum* | P192 | ● | ◐ | ◐ | — |

> ● 産する　◐ 一部に産する　— 産しない

---

## 開発状況

### 完了

- [x] 検索ツリー実装（全20種・全分岐網羅）
- [x] 種情報データ整備（学名・参照ページ・節・分布）
- [x] 分布バッジ表示（産する地域のみ表示）
- [x] 候補種のプレビュー表示（選択肢ごとに到達できる種を列挙）
- [x] 戻る・リセット機能
- [x] パンくずナビゲーション
- [x] PWA対応（manifest・Service Worker・オフライン動作）
- [x] PWAアイコン作成（ネコノメソウ属の形態をモチーフ）
- [x] しだアプリと同一のファイル設計・CSS設計で統一
- [x] GitHub Pages 公開（手動デプロイ）

### 今後の課題・検討事項

- [ ] 変種・品種の追加（ハナネコノメ、ヨゴレネコノメ、ミチノクネコノメなど主要変種）
- [ ] 各種の写真リンク・画像表示
- [ ] 開花時期カレンダーの追加
- [ ] 検索ツリーに補足注記（形態の説明・見分けのポイント）を追加
- [ ] 染色体数の表示
- [ ] しだアプリとの統合（同一リポジトリ・ポータルページ）

---

## 設計メモ

### しだアプリとの差分

| 項目 | しだアプリ | ネコノメソウアプリ |
|------|-----------|-----------------|
| 対象 | 複数属（26属） | 単属（*Chrysosplenium*） |
| 結果表示 | 種名のみ | 種名＋学名＋参照ページ＋分布バッジ |
| 種情報 | なし | `SPECIES_DATA` グローバルに格納 |
| データ構造 | `GENUS_TREES` のみ | `GENUS_TREES` ＋ `SPECIES_DATA` |
| ヒント表示 | なし | 節・列名をヒントとして各ノードに付加 |

### 属データ追加の手順

他の属を追加する場合:

1. `data/{属ID}.js` を作成し `GENUS_TREES` と `SPECIES_DATA` に書き込む
2. `data/genera.js` の `GENERA_DATA` 配列に属情報を1件追加
3. `sw.js` の `PRECACHE_FILES` に `./data/{属ID}.js` を追記
4. `sw.js` の `CACHE_VERSION` を更新（例: `v1` → `v2`）
5. コミット＆プッシュ

---

*最終更新: 2026年5月*